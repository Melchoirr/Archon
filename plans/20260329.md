* _scan_existing_outputs的问题：具体是什么结构和功能？出现了什么问题，为什么？
  * 本质上扫描了这个agent理应产生的东西（存疑）然后注入给这个agent本身，感觉并不是跳过步骤的根本原因
  * 是需要有这个功能的，应该和下一轮次读取信息的功能混用吗？应该直接统一处理。保证commit应该没问题
  * 注册在base agent合理吗？
* 版本维护
  * commit：每个agent应该维护自己的输出文件。最好是每个agent的输出单独在一个目录里面，或者可以track。agent运行结束最后一步commit，包含自己是谁以及版本。对于中断和循环问题不大
  * 上传kb也类似
* fsm
  * 顺序运行
  * 分支
    * 判别agent
  * 断点继续
  * 循环

## agents

* topic部分
  * elaborate - survey - ideation
  * 链条太长，重新survey太耗时
* idea部分
  * 一次性输出3个md不太合理，不能保证一致性
* 环境管理
* 路径管理
* coding agent
  * 编码质量
* experiment agent
* code review
* 没有选取/读取功能，定位不到关键信息？
* claude -p带有检索/修改文件的全套cc功能吗？
* prompt enhancer?
* 可拆卸的模块
  * 不能写死？
* 限制并行都是3个吗？
* 看一看中间的message？
* 对于读取失败，怎么办